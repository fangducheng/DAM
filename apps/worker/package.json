{
  "name": "@dam/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "node --max-old-space-size=384 --enable-source-maps dist/main.js",
    "dev:watch": "concurrently -k -n tsc,node \"tsc -p tsconfig.build.json --watch --preserveWatchOutput\" \"node --max-old-space-size=384 --watch --enable-source-maps dist/main.js\"",
    "start": "node --enable-source-maps dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests --maxWorkers=1 --no-file-parallelism"
  },
  "dependencies": {
    "@dam/config": "workspace:*",
    "@dam/database": "workspace:*",
    "@nestjs/common": "^11.1.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/core": "^11.1.0",
    "minio": "^8.0.0",
    "nestjs-pino": "^4.4.0",
    "pino": "^9.9.0",
    "pino-http": "^11.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "pino-pretty": "^13.1.0",
    "tsx": "^4.20.0"
  }
}
