# Development Progress - 2026-08-05

## Completed

- Restored Docker Desktop and the constrained PostgreSQL, Redis, and MinIO profile.
- Fixed NestJS bootstrap logging to use the logger registered by `nestjs-pino`.
- Added the runtime packages required by Nest validation and Swagger on Fastify.
- Simplified the Redis readiness probe to rely on the typed ioredis `PING` contract.
- Confirmed the database migration is current and all three containers are healthy.
- Verified liveness, readiness, and Swagger against a production build of the API.
- Rendered the Vue operations console at desktop and mobile widths with all dependencies up.
- Passed formatting, lint, Prisma validation, all workspace type checks, three unit tests, and
  all production builds after the runtime fixes.
- Approved the Tenant, legal organization, business space, RBAC, inherited ACL, MFA, and API
  error-handling design and its implementation plan.
- Expanded the Prisma model for Tenants, organization memberships, shared-service groups,
  Tenant/organization-owned spaces, invitations, MFA recovery codes, rotating sessions, and
  Tenant-aware authorization records.
- Added database checks, partial unique indexes, and cross-Tenant triggers for memberships,
  groups, roles, spaces, role bindings, space members, invitations, and resource ACL entries.
- Added 15 stable permission codes, nine system role templates, an idempotent seed, and the
  first Nest authorization policy with DENY-first and default-deny behavior.
- Migrated Prisma CLI configuration to `prisma.config.ts` so local commands load the root
  environment consistently without the deprecated package configuration.

## Runtime Verification

- Liveness returned `ok` from `GET /api/v1/health/live`.
- Readiness returned `ready` with PostgreSQL, Redis, and MinIO all reporting `up`.
- Swagger returned HTTP 200 from `/api/docs`.
- The operations console returned HTTP 200 and rendered without horizontal overflow or text
  overlap at 1440 x 900 and 390 x 844.
- All three migrations deployed successfully to a disposable database and the development
  database; migration status is current.
- Cross-Tenant constraint smoke tests rejected invalid organization memberships, group members,
  space owners, role bindings, space members, ACL principals, and Tenant reassignment.
- Running the seed twice produced exactly 15 permissions, nine system roles, and 41 role-to-
  permission relationships.
- The full workspace verification passed: format, lint, Prisma validation, type checks, 11 unit
  tests, and all production builds.

## Next Checkpoint

Implement the identity lifecycle: bootstrap command, invitations, Argon2id passwords, TOTP and
recovery codes, JWT access tokens, rotating refresh sessions, cookie transport, and Nest guards.
