# Development Progress - 2026-08-06

## Completed

- Implemented current-Tenant information and security-policy APIs.
- Implemented organization creation, hierarchy, status, membership, primary-employment, and
  organization-role management.
- Added last-organization-administrator protection and synchronized organization role bindings.
- Implemented Tenant-wide and organization-scoped groups with member management.
- Migrated invitation authorization checks to the shared authorization service.
- Added transactional audit events and `authorizationVersion` increments to authorization writes.
- Implemented Tenant-owned shared spaces and organization-owned private spaces.
- Implemented direct USER, GROUP, and ORGANIZATION space memberships with `SpaceManager`,
  `Editor`, `Contributor`, `Viewer`, and `Restricted` roles.
- Added last-active-SpaceManager protection and removal of a member's dormant space ACL entries.
- Implemented direct and inherited folder/file ACL management for USER, GROUP, and ORGANIZATION
  principals, including expiry, DENY precedence, and permission-source explanations.
- Added `canEnterSpace` so Restricted members can enter a space without receiving default content
  access.
- Added complete route-parameter DTOs for strict Nest validation on member and permission routes.
- Added the Vue Router-based management console with an authenticated desktop shell and mobile
  bottom navigation.
- Added login, MFA, invitation acceptance, recovery-code display, and session management flows.
- Kept access tokens in memory and added refresh-cookie session recovery, concurrent refresh
  deduplication, and automatic retry after an expired access token.
- Added organization, member, invitation, shared-group, business-space, space-member, ACL, and
  inherited permission-source management views.
- Added user-facing API error handling for validation errors, expired sessions, denied or hidden
  resources, stale writes, expired invitations, rate limits, and server request IDs.
- Added API-client tests for field-error mapping and concurrent 401 refresh behavior.
- Added a Playwright Core verification flow using the locally installed Microsoft Edge browser.
- Added one hidden root folder per space and database-enforced uniqueness for active sibling names.
- Implemented authorized folder browsing, breadcrumb generation, creation, rename, move, optimistic
  locking, deletion batches, recycle-bin listing, and exact-batch restoration.
- Implemented resumable MinIO multipart upload sessions with quota reservation, short-lived part
  URLs, ETag recording, size validation, streaming SHA-256 verification, abort, and expiry handling.
- Added immutable asset versions, current-version selection, space-usage accounting, and
  authorization-checked short-lived preview and download URLs.
- Added a local processing bypass that records `SKIPPED` scan status and is rejected by production
  environment validation; the deferred mode retains quarantine state for the processing worker.
- Added the responsive asset workspace with space selection, capacity, breadcrumbs, folder and
  recycle views, three-part-concurrency upload progress, preview, download, rename, ACL navigation,
  version history, current-version switching, and version uploads.

## Runtime Verification

- PostgreSQL, Redis, and MinIO are healthy under the constrained local Docker Compose profile.
- The Tenant/organization/group integration test passes against a disposable migrated database.
- The A/B private-space and shared-group ACL integration test passes against a disposable database.
- The Space/ACL test covers organization isolation, Restricted entry, inherited ALLOW, direct DENY
  precedence, ACL cleanup, cross-Tenant rejection, and last-SpaceManager protection.
- `pnpm verify` passes formatting, ESLint, Prisma validation, all six workspace type checks, regular
  tests, and all production builds.
- The production API build reports `ready`; PostgreSQL, Redis, and MinIO each report `up`.
- Swagger returns HTTP 200 and exposes the new `/api/v1/spaces` and
  `/api/v1/resource-nodes/{nodeId}` membership, ACL, and permission-explanation routes.
- The disposable Space/ACL test database was removed after verification.
- The management console passes Vue type checking, two API-client tests, and its production build.
- The Edge verification flow passes real login and MFA across status, organization, space, and
  permission views at 1440x900 and 390x844 without page overflow, console errors, or unexpected
  HTTP responses.
- Responsive screenshots were reviewed for navigation, form, table, action, and notification
  placement; the disposable UI verification Tenant was removed afterward.
- The asset integration test passes against disposable PostgreSQL and real MinIO with a 9 MiB
  two-part upload, SHA-256 verification, a second immutable version, preview content verification,
  optimistic locking, recycle-bin deletion, and restoration.
- The Edge verification flow creates a folder and uploads a real 9 MiB two-part file through the
  asset workspace, opens version history, and verifies 1440x900 and 390x844 layouts without page
  overflow, console errors, or unexpected HTTP responses.
- All disposable asset databases, UI verification Tenants, external test objects, and orphaned
  storage metadata were removed after verification.

## Local-First Scope

- Continue running PostgreSQL, Redis, MinIO, API, and Web locally.
- Keep RabbitMQ and ClamAV optional on the 8 GB development machine.
- Keep database, cache, object-storage, cookie, and external-service boundaries configurable by
  environment variables.
- Defer private-server topology, Kubernetes, public TLS, and high-availability work until the core
  local product workflows are complete.

## Next Checkpoint

Begin the processing and discovery slice: ClamAV scanning, metadata extraction, preview renditions,
tags, PostgreSQL full-text search, audit browsing, and user notifications. Keep processing services
optional on the 8 GB workstation and preserve quarantine whenever local bypass is disabled.
